name: Example Workflow

on:
  - pull_request

jobs:
  hello_world_job:
    runs-on: ubuntu-latest
    name: A job to say hello
    steps:
      - name: Hello world action step
        uses: MrRobz/dt-hotfix-notify@pr-4
        with:
          channel: 'Mona the Octocat'